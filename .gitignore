node_modules/
dist/
.DS_Store
.env

# Pipeline caches: rebuilt by the refresh job, restored from Actions cache.
# candidates.json is committed too. The Actions cache is empty after a
# cancelled run, and discovery would otherwise start from zero every day.
data/repos.json
data/verified.json
data/classified.json
data/health.json
.wrangler/
.dev.vars
